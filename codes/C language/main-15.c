// Displaying Fibonacci Series
#include <stdio.h>
int main()
{
    int i,n,nextTerm,secondTerm;
    printf("enter a number: ");
    scanf("%d",&i);
        secondTerm==i;
        nextTerm==i+secondTerm;
        printf("Enter number of turns": );
        scanf("%d",&n);
        
        return 0;
}